    <%# Left side navbar %>
<nav class="navbar navbar-expand-lg navbar-light bg-transparent fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand text-secondary" href="/">VINTEDGE</a>
    <%# <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent"> %>
    <%# Right side navbar %>
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
     </ul>
      <ul class="navbar-nav me-5 mb-2 mb-lg-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            PROFILE
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%# Customising the views for different users  %>
           <% if user_signed_in? %>
              <li><a class="dropdown-item" href="/my_listings">My listings</a></li>
            <% end %>
            <% if user_signed_in? %>
              <li><%= link_to "Edit profile", edit_user_registration_path, class: "dropdown-item" %></li>
            <% else %>
              <li><%= link_to "Sign up", new_user_registration_path, class: "dropdown-item" %></li>
              <li><%= link_to "Log in", new_user_session_path, class: "dropdown-item" %></li>
            <% end %>
              <li role="separator" class="divider"></li>
            <% if user_signed_in? %>
              <li><%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
          <%# Show admins only  %>
            <% if has_role? :admin %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  ADD LISTING</a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <%= link_to 'Admin: Show all', listings_path, class: "dropdown-item" %>
                  <%= link_to 'Create new listing', new_listing_path, class: "dropdown-item" %>
                </ul>
              </li>
              <% else %>
              <%# Show add listing option when signed in  %>
                <% if user_signed_in? %>
                  <%= link_to 'ADD LISTING', new_listing_path, class: "nav-item nav-link" %>
              <% end %>
            <% end %>
          <li class="nav-item">
            <a class="nav-link"  href="/about">ABOUT US</a>
          </li>      
        </div>
      </ul>
    </div>
  </div>
</nav>